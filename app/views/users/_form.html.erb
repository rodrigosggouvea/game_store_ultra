<%= form_for @user, :class=>"form-horizontal" do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nome" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ocupation, "Profissão" %>
    <%= f.text_field :ocupation %>
  </div>
  <div class="field">
    <%= f.label :bio, "Biografia" %>
    <%= f.text_field :bio %>
  </div>
  <div class="actions">
    <%= f.submit "Atualizar" %>
  </div>
<% end %>
