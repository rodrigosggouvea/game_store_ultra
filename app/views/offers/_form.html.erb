<%= nested_form_for @offer, :class=>"form-horizontal" do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Título" %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :price, "Preco" %>
    <%= f.text_field :price, :id => "dindin" %>
  </div>
  <div class="field">
    <%= f.label :text, "Texto" %>
    <%= f.text_area :text %>
  </div>
  <div class="images">
    <h3>Imagens</h3>
    <%= f.fields_for :offer_images do |offer_image| %>
      <div class="field">
        <%= offer_image.file_field :image %>
        <%= offer_image.link_to_remove "Remover Imagem" %>
      </div>
    <% end %>
    <%= f.link_to_add "Adicionar Imagem", :offer_images %>    
  </div>
  <div class="actions">
    <%= f.submit "Salvar", :class => "btn-primary btn-large" %>
  </div>
<% end %>

<script type="text/javascript">
    $(document).ready(function(){
        $("#dindin").maskMoney({showSymbol:true, symbol:"R$", decimal:",", thousands:"."});
    });
</script>
