<h1>Lista de Ofertas</h1>

<% if @offers.any? %>
<table>
  <tr>
    <th>Usuário</th>
    <th>Plataforma</th>
    <th>Preco</th>
    <th>Texto</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @offers.each do |offer| %>
  <tr>
    <td><%= offer.user_id %></td>
    <td><%= offer.platform_title_id %></td>
    <td><%= offer.price %></td>
    <td><%= offer.text %></td>
    <td><%= link_to 'Exibir', offer %></td>
    <td><%= link_to 'Editar', edit_offer_path(offer) %></td>
    <td><%= link_to 'Remover', offer, confirm: 'Tem Certeza?', method: :delete %></td>
  </tr>
<% end %>
</table>
<% end %>

<br />

<%= link_to 'Nova Oferta', new_offer_path %>
