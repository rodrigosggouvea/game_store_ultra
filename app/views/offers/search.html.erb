<h1>Pesquisar</h1>
<%= form_for @offers, :url => search_offers_path, :html => {:method => :get} do |f| %>
  <%= f.label :title_or_text_contains, "Título ou conteúdo do texto" %>
  <%= f.text_field :title_or_text_contains %><br />
  <%= f.submit "Pesquisar", :class => "btn-primary btn-large" %>
<% end %>
<hr/>
<% if @offers %>
	<table class = "table">
		<% @offers.each do |offer| %>
			<tr>
				<td><%= link_to offer.title, offer %></td>
				<td><%= link_to offer.price, offer %></td>
			</tr>
			<hr\>
		<% end %>
	</table>
<% end %>